# Copy to .env and fill in your values:  cp .env.example .env
# HuggingFace token — enables higher download rate limits / gated models.
# Create one at https://huggingface.co/settings/tokens (a "read" token is enough).
HF_TOKEN=
